/**
 * Single source of truth for each view's default inputs.
 *
 * These values are consumed in three places that used to each hard-code them
 * (which let them drift): the view component's `usePersistentState` initials, the
 * `run.ts` orchestration fallbacks, and — indirectly, via the request schemas —
 * the API routes. Import from here instead of repeating literals.
 */

export const DEFAULTS = {
  tax: {
    startAge: 62,
    filing: "mfj" as "single" | "mfj",
    years: 30,
    taxable: 0,
    taxableBasisPct: 0.6,
    deferred: 1_200_000,
    roth: 150_000,
    annualSpend: 60_000,
    otherIncome: 30_000,
    nominalReturn: 0.06,
    inflation: 0.025,
    ltcgRate: 0.15,
    conversionTopRate: 0.12,
    terminalTaxRate: 0.24,
  },
  care: {
    startAge: 65,
    startingBalance: 1_000_000,
    baseSpend: 45_000,
    realReturn: 0.035,
    vol: 0.1,
    actToAssisted: 0.03,
    actToSkilled: 0.005,
    actToDead: 0.012,
    asstToSkilled: 0.1,
    asstToDead: 0.08,
    asstToActive: 0.05,
    skilledToDead: 0.25,
    ageRamp: 0.05,
    assistedCost: 60_000,
    skilledCost: 110_000,
    nSims: 10_000,
  },
  longevity: {
    ageA: 65,
    sexA: "male" as "male" | "female",
    couple: false,
    ageB: 63,
    sexB: "female" as "male" | "female",
    longevityAdj: 0,
    startingBalance: 1_000_000,
    annualSpend: 45_000,
    realReturn: 0.035,
    vol: 0.1,
    survivorSpend: 0.75,
    nSims: 10_000,
  },
  seqrisk: {
    startingBalance: 1_000_000,
    retirementYears: 30,
    annualSpend: 35_000,
    inflation: 0.025,
    equityMean: 0.07,
    equityVol: 0.16,
    bufferYield: 0.03,
    bearYears: 3,
    bearMean: -0.05,
    bearVol: 0.2,
    troughDrawdown: 0.1,
    refillBuffer: true,
    maxBufferYears: 8,
    targetSellProb: 0.05,
    nSims: 6_000,
  },
  dyn: {
    startingBalance: 1_000_000,
    retirementYears: 30,
    initialRate: 0.05,
    meanReturn: 0.06,
    stdReturn: 0.12,
    inflation: 0.025,
    guardBand: 0.2,
    guardAdjust: 0.1,
    ratchetThreshold: 0.5,
    ratchetStep: 0.1,
    ratchetEvery: 3,
    nSims: 8_000,
  },
};
